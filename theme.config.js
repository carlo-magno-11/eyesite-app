/** @type {const} */
const themeColors = {
  primary: { light: '#C9A84C', dark: '#C9A84C' },       // Dorado Eyesite
  background: { light: '#0D0D0D', dark: '#0D0D0D' },    // Negro profundo
  surface: { light: '#1A1A1A', dark: '#1A1A1A' },       // Gris oscuro para tarjetas
  foreground: { light: '#F5F5F5', dark: '#F5F5F5' },    // Blanco cálido
  muted: { light: '#9A9A9A', dark: '#9A9A9A' },         // Gris para texto secundario
  border: { light: '#2A2A2A', dark: '#2A2A2A' },        // Borde sutil
  success: { light: '#4ADE80', dark: '#4ADE80' },       // Verde para rendimiento alto
  warning: { light: '#FBBF24', dark: '#FBBF24' },       // Amarillo para rendimiento medio
  error: { light: '#F87171', dark: '#F87171' },         // Rojo para errores
};

module.exports = { themeColors };
